import React from 'react'
import { items } from '../../../../data/list'

const Detail = ({id}) => {
    let imgPath = items[id]['model']
    // console.log(imgPath)
    return (
    <div style={{display: "flex", flexDirection: "row"}}>
        
    </div>
    )
}

export default Detail